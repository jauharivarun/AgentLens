# Product guide

AgentLens is a two-page developer tool:

1. Agent — run a tool-using model against a demo workspace.
2. Observability — inspect tokens, tools, latency, estimated cost, and outcomes.

Model selection is UI state. The task prompt stays model-agnostic.

Supported demo task types include coding, RAG question answering, summarization, and general Q&A.
